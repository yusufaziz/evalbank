/**
 * @brief Enum representing the possible judgements for evaluations.
 */
export enum EVALUATION_JUDGEMENT {
  NOT_EXECUTED = 0, // Evaluation has not been executed
  NOT_SUPPORT = 1, // Evaluation is not supported
  NG = 2, // Evaluation failed
  OK = 3, // Evaluation passed
}
